global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: movi-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - movi-backend:8080
